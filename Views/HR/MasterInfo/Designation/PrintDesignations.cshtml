@model IEnumerable<Exampler_ERP.Models.Settings_DesignationType>

<!DOCTYPE html>
<html>
<head>
    <title>Print Designationes</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
        }

        table, th, td {
            border: 1px solid black;
        }

        th, td {
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: white;
        }
    </style>
</head>
<body style="background-color:white">
    <h2>Designation List</h2>
    <table>
        <thead>
            <tr>
                <th>Designation ID</th>
                <th>Designation Name</th>
                <th>Active</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var Designation in Model)
            {
                <tr>
                    <td>@Designation.DesignationTypeID</td>
                    <td>@Designation.DesignationTypeName</td>
                    <td>@(Designation.ActiveYNID == 1 ? "Yes" : "No")</td>
                </tr>
            }
        </tbody>
    </table>
    <script>
        window.print();
    </script>
</body>
</html>
