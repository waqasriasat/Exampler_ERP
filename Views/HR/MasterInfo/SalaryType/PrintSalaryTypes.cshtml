@model IEnumerable<Exampler_ERP.Models.Settings_SalaryType>
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
<!DOCTYPE html>
<html>
<head>
    <title>Print Salary Typees</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
        }

        table, th, td {
            border: 1px solid black;
        }

        th, td {
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: white;
        }
    </style>
</head>
<body style="background-color:white">
    <h2>Salary Type List</h2>
    <table>
        <thead>
            <tr>
                <th>Salary Type ID</th>
                <th>Salary Type Name</th>
                <th>@Localizer["lbl_Active"]</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var SalaryType in Model)
            {
                <tr>
                    <td>@SalaryType.SalaryTypeID</td>
                    <td>@SalaryType.SalaryTypeName</td>
                    <td>@(SalaryType.ActiveYNID == 1 ? "Yes" : "No")</td>
                </tr>
            }
        </tbody>
    </table>
    <script>
        window.print();
    </script>
</body>
</html>
