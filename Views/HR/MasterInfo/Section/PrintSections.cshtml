@model IEnumerable<Exampler_ERP.Models.Settings_SectionType>
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
<!DOCTYPE html>
<html>
<head>
	<title>@Localizer["lbl_Print"] @Localizer["lbl_Section"]</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
        }

        table, th, td {
            border: 1px solid black;
        }

        th, td {
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: white;
        }
    </style>
</head>
<body style="background-color:white">
	<h2>@Localizer["lbl_Section"] @Localizer["lbl_List"]</h2>
    <table>
        <thead>
            <tr>
                <th>Section ID</th>
                <th>@Localizer["lbl_Department"] @Localizer["lbl_Name"]</th>
                <th>@Localizer["lbl_Section"] @Localizer["lbl_Name"]</th>
                <th>@Localizer["lbl_Active"]</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var Section in Model)
            {
                <tr>
                    <td>@Section.SectionTypeID</td>
                    <td>@(Section.DepartmentType?.DepartmentTypeName ?? "N/A")</td>
                    <td>@Section.SectionTypeName</td>
                    <td>@(Section.ActiveYNID == 1 ? "Yes" : "No")</td>
                </tr>
            }
        </tbody>
    </table>
    <script>
        window.print();
    </script>
</body>
</html>
