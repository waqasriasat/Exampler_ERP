@model Exampler_ERP.Models.HR_Contract

<div class="modal fade" id="editContractModal" tabindex="-1" role="dialog" aria-labelledby="editContractModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="editContractModalLabel">Edit Contract</h5>
                <button type="button" class="btn btn-danger CloseButton" data-dismiss="modal" id="Close">
                    x
                </button>
            </div>
            <div class="modal-body">
                <form id="editContractForm" method="post" asp-action="Edit" asp-controller="Contract" enctype="multipart/form-data">
                    @Html.Partial("~/Views/HR/Employeement/Employee/_EditContract.cshtml", Model)
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal" id="Close">Close</button>
                <button type="button" class="btn btn-primary" id="saveContractChanges">Save Changes</button>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    $(document).ready(function () {
        $('#contractTypeSelect').change(function () {
            var selectedContractType = $(this).val();
            if (selectedContractType == '1') { // Show the date fields if ContractType is 1
                $('#dateSection').show();
            } else { // Hide the date fields for other ContractTypes
                $('#dateSection').hide();
            }
        });

        // Trigger change event on page load to ensure correct state
        $('#contractTypeSelect').trigger('change');
    });
</script>
