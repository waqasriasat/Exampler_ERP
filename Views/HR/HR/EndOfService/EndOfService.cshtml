@model IEnumerable<Exampler_ERP.Models.HR_EndOfService>
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["lbl_HumanResources"] + " -  " + Localizer["lbl_EndOfService"];
        }

@section PageScripts {
    <script>
        $(document).ready(function () {
            // Edit EndOfService modal popup
            $('#EndOfServiceListContainer').on('click', '.editEndOfServiceLink', function (e) {
                e.preventDefault();
                var EndOfServiceId = $(this).data('id');
                $.get('@Url.Action("Edit", "EndOfService")', { id: EndOfServiceId }, function (data) {
                    $('#editEndOfServiceModalContainer').html(data);
                    $('#editEndOfServiceModal').modal('show');
                });
            });
            $('#editEndOfServiceModalContainer').on('click', '#saveEndOfServiceChanges', function () {
                var formData = $('#editEndOfServiceForm').serialize();
                $.post('@Url.Action("Edit", "EndOfService")', formData, function (response) {
                    if (response.success) {
                            $('#editEndOfServiceModal').modal('hide');

                            // ✅ Ab poora list AJAX se reload karo
                            $.get('@Url.Action("Index", "EndOfService")', function (partialHtml) {
                                var html = $(partialHtml).find('#EndOfServiceListContainer').html();
                                $('#EndOfServiceListContainer').html(html);
                            });
                        }
                });
            });


            // Add new EndOfService modal popup
            $('.addEndOfServiceLink').click(function () {
                $.get('@Url.Action("Create", "EndOfService")', function (data) {
                    $('#addEndOfServiceModalContainer').html(data);
                    $('#addEndOfServiceModal').modal('show');
                });
            });

            // Save new EndOfService form
            $('#addEndOfServiceModalContainer').on('click', '#saveNewEndOfService', function () {
                var formData = $('#addEndOfServiceForm').serialize();
                $.post('@Url.Action("Create", "EndOfService")', formData, function (response) {
                   if (response.success) {
                            $('#addEndOfServiceModal').modal('hide');

                            // ✅ Ab poora list AJAX se reload karo
                            $.get('@Url.Action("Index", "EndOfService")', function (partialHtml) {
                                var html = $(partialHtml).find('#EndOfServiceListContainer').html();
                                $('#EndOfServiceListContainer').html(html);
                            });
                        }
                });
            });

            // Close Modal Add Contrainer
            $('#addEndOfServiceModalContainer').on('click', '#Close', function () {
                $('#addEndOfServiceModal').modal('hide'); // Close the modal directly
                // Additional logic if needed
            });

            // Close Modal Edit Contrainer
            $('#editEndOfServiceModalContainer').on('click', '#Close', function () {
                $('#editEndOfServiceModal').modal('hide'); // Close the modal directly
                // Additional logic if needed
            });

            // Delete EndOfService
             $('#EndOfServiceListContainer').on('click', '.deleteEndOfServiceLink', function (e) {
                e.preventDefault();
                var EndOfServiceId = $(this).data('id');

                // Show confirmation dialog
                if (confirm('Are you sure you want to delete this EndOfService?')) {
                    $.post('@Url.Action("Delete", "EndOfService")', { id: EndOfServiceId }, function (response) {
                        if (response.success) {
                    $.get('@Url.Action("Index", "EndOfService")', function (partialHtml) {
                      var html = $(partialHtml).find('#EndOfServiceListContainer').html();
                      $('#EndOfServiceListContainer').html(html);
                    });
                  }
                    });
                } else {
                    // If user clicks 'No', do nothing
                }
            });

            // Print functionality
            $('#printButton').click(function () {
                window.print();
            });
            $.widget("custom.autocomplete", $.ui.autocomplete, {
                _renderItem: function (ul, item) {
                    return $("<li>")
                        .append(item.label)  // Render the HTML content in `label`
                        .appendTo(ul);
                }
            });

            $("#EmployeeName").autocomplete({
                source: function (request, response) {

                    $.ajax({
                        url: '@Url.Action("GetEmployeeSuggestions", "Employee")', // Ensure this matches the C# method
                        type: 'GET',
                        data: { term: request.term },
                        success: function (data) {

                            response($.map(data, function (item) {
                                return {
                                    label: item.label,  // HTML content to be displayed in suggestions
                                    id: item.id,     // Employee ID for the hidden input
                                    name: item.name,      // Full name to be used in the input field
                                    value: item.name
                                };
                            }));
                        }
                    });
                },
                minLength: 2,
                select: function (event, ui) {
                    
                    // Set the selected employee's ID and name
                    $("#EmployeeID").val(ui.item.id); // Set the hidden input for Employee ID
                    $("#EmployeeName").val(ui.item.name); // Set the input field for Employee Name
                }
            });
			$('#searchEmployeeForm').submit(function (e) {
				e.preventDefault(); // Prevent full page reload

				var form = $(this);
				var actionUrl = form.attr('action');

				// Serialize form data
				var formData = form.serialize();

				$.get(actionUrl, formData, function (partialHtml) {
					// Load only the relevant part of the response
					var html = $(partialHtml).find('#EndOfServiceListContainer').html();
					$('#EndOfServiceListContainer').html(html);
				});
			});
        });
    </script>
    <script>
        function printEndOfServices() {
            var printWindow = window.open('@Url.Action("Print", "EndOfService")', '_blank');
            printWindow.focus();
        }

        function closeModal() {
            $('.close').click(function () {
                $('#addEndOfServiceModal').modal('hide');
                $('#editEndOfServiceModal').modal('hide');
            });
        }
    </script>
}

<h4 class="py-3 mb-4">
    <span class="text-muted fw-light">@Localizer["lbl_HumanResources"] /</span>  @Localizer["lbl_EndOfService"]
</h4>
<form id="searchEmployeeForm" method="get" action="@Url.Action("Index", "EndOfService")">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <h5 class="card-header">@Localizer["lbl_SearchingBox"]</h5>
                <div class="Searching-Box-padding" style="padding-bottom: 15px;">
                    <div class="row mb-4">
                        <!-- From Date Field -->
                        <div class="col-md-3 mb-3">
                            <label class="form-label fw-bold">@Localizer["lbl_FromDate"]</label>
							<input class="form-control" type="date" name="FromDate" id="FromDate" value="@ViewBag.FromDate?.ToString("yyyy-MM-dd")" />
                        </div>

                        <!-- To Date Field -->
                        <div class="col-md-3 mb-3">
                            <label class="form-label fw-bold">@Localizer["lbl_ToDate"]</label>
							<input class="form-control" type="date" name="ToDate" id="ToDate" value="@ViewBag.ToDate?.ToString("yyyy-MM-dd")" />
                        </div>

                        <!-- Employee Name Search Field -->
                        <div class="col-md-3 mb-3">
                            <label class="form-label fw-bold">@Localizer["lbl_EmployeeName"]</label>
                            <input type="text" class="form-control" id="EmployeeName" placeholder="@Localizer["lbl_SearchEmployee"]" name="EmployeeName" value="@ViewBag.EmployeeName" />
                            <input type="hidden" class="form-control" id="EmployeeID" placeholder="@Localizer["lbl_SearchEmployee"]" name="EmployeeID" value="@ViewBag.EmployeeID" />
                        </div>

                        <!-- EndOfServiceReason Type Dropdown -->
                        <div class="col-md-3 mb-3">
                            <label class="form-label fw-bold">@Localizer["lbl_ReasonType"]</label>
							<select name="EndOfServiceReasonTypeID" id="EndOfServiceReasonTypeID" class="form-control">
                                @foreach (var item in ViewBag.EndOfServiceReasonTypeList)
                                {
                                    if (item.Value == ViewBag.EndOfServiceReasonTypeID?.ToString())
                                    {
                                        <option value="@item.Value" selected>@item.Text</option>
                                    }
                                    else
                                    {
                                        <option value="@item.Value">@item.Text</option>
                                    }
                                }
                            </select>
                        </div>

                    </div>

                    <!-- Action Buttons -->
                    <div class="row">
                        <div class="col-md-12 text-end">
                            <button type="submit" class="btn btn-primary">@Localizer["lbl_Search"]</button>
                            <button type="button" class="btn btn-primary addEndOfServiceLink">@Localizer["lbl_AddNew"]</button>
                            <!-- Example: Add buttons for exporting to Excel and printing -->
                            <button type="button" class="btn btn-outline-secondary" onclick="location.href='@Url.Action("ExportToExcel", "EndOfService")'">@Localizer["lbl_Excel"]</button>
                            <button type="button" class="btn btn-outline-secondary" onclick="printEndOfServices()">@Localizer["lbl_Print"]</button>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</form>
<br />
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <h5 class="card-header">@Localizer["lbl_EndOfServiceList"]</h5>
            <div class="">
                <div class="col-lg-12 col-md-12 col-sm-12 mb-12 SmallHide GVHeader">
                    <div class="row">
                        <div class="col-10 col-md-11 col-lg-9">
                            <div class="row">
                                <div class="col-4">
                                    <p class="text-nowrap">@Localizer["lbl_EmployeeName"]</p>
                                </div>
                                <div class="col-4">
                                    <p class="text-nowrap">@Localizer["lbl_DateOfCompletionOfWork"]</p>
                                </div>
                                <div class="col-4">
                                    <p class="text-nowrap">@Localizer["lbl_TotalEndOfServiceDues"]</p>
                                </div>
                            </div>
                        </div>
                        <div class="col-4 col-md-1 col-lg-3">
                            <div class="row">
                                <div class="col-12 col-md-12 col-lg-4">
                                    <p class="text-nowrap">@Localizer["lbl_Edit"]</p>
                                </div>
                                <div class="col-12 col-md-12 col-lg-4">
                                    <p class="text-nowrap">@Localizer["lbl_Print"] </p>
                                </div>
                                <div class="col-12 col-md-12 col-lg-4">
                                    <p class="text-nowrap">@Localizer["lbl_Delete"]</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div id="EndOfServiceListContainer">
                    @Html.Partial("~/Views/HR/HR/EndOfService/_EndOfServiceListPartial.cshtml", Model)
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Include the modal partial -->
<div id="editEndOfServiceModalContainer"></div>
<div id="addEndOfServiceModalContainer"></div>
