@model Exampler_ERP.Models.HR_VacationSettle

@{
    var EmployeeName = Context.Session.GetString("EmployeeName");
}

<div class="modal fade" id="editVacationSettleModal" tabindex="-1" role="dialog" aria-labelledby="editVacationSettleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <!-- Modal Header -->
            <div class="modal-header bg-warning text-white">
                <h5 class="modal-title" id="editVacationSettleModalLabel">
                    <i class="fas fa-edit me-2"></i>Edit VacationSettle
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <!-- Modal Body -->
            <div class="modal-body">
                <!-- Edit VacationSettle Form -->
                <form id="editVacationSettleForm" method="post" asp-action="Edit" asp-controller="VacationSettle" enctype="multipart/form-data">
                    @Html.Partial("~/Views/EmployeePortal/Apply/VacationSettle/_EditVacationSettle.cshtml", Model)
                </form>

                <!-- Success Message -->
                <div id="successMessage" class="alert alert-success text-center mt-3" style="display:none;"></div>
            </div>

            <!-- Modal Footer -->
            <div class="modal-footer border-0">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times-circle"></i> Close
                </button>
                <button type="submit" id="saveVacationSettleChanges" class="btn btn-success">
                    <i class="fas fa-check-circle"></i> Save Changes
                </button>
            </div>
        </div>
    </div>
</div>
