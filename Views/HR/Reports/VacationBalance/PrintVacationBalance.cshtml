@model IEnumerable<Exampler_ERP.Models.Temp.VacationBalanceViewModel>
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    Layout = "_PrintLayout";
}
<!DOCTYPE html>
<html>
<head>
    <title>@Localizer["lbl_PrintVacationBalance"]</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
        }

        table, th, td {
            border: 1px solid black;
        }

        th, td {
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: white;
        }
    </style>
</head>
<body style="background-color:white">
    <h2>@Localizer["lbl_VacationBalanceList"]</h2>
    <table>
        <thead>
            <tr>
                <th>@Localizer["lbl_EmployeeName"]</th>
                <th>@Localizer["lbl_TotalBalance"]</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var VacationBalance in Model)
            {
                <tr>
                    <td>@VacationBalance.EmployeeName</td>
                    <td>@VacationBalance.TotalBalance</td>
                </tr>
            }
        </tbody>
    </table>
    <script>
        window.print();
    </script>
</body>
</html>
