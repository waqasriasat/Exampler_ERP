@model IEnumerable<Exampler_ERP.Models.ST_MaterialReceived>
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
<!DOCTYPE html>
<html>
<head>
    <title>@Localizer["lbl_Print"]  Material Received</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
        }

        table, th, td {
            border: 1px solid black;
        }

        th, td {
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: white;
        }
    </style>
</head>
<body style="background-color:white">
    <h2>Material Received List</h2>
    <table>
        <thead>
            <tr>
                <th>Received @Localizer["lbl_ID"]</th>
                <th>Received @Localizer["lbl_Date"]</th>
                <th>@Localizer["lbl_ItemName"]</th>
                <th>Quantity</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var MaterialReceived in Model)
            {
                <tr>
                    <td>@MaterialReceived.MaterialReceivedID</td>
                    <td>@MaterialReceived.MaterialReceivedDate.ToString("dd/MM/yyyy")</td>
                    <td>@(MaterialReceived.Item?.ItemName)</td>
                    <td>@(MaterialReceived.Quantity)</td>
                </tr>
            }
        </tbody>
    </table>
    <script>
        window.print();
    </script>
</body>
</html>
